// import React from 'react';
// import * as ReactIcons from 'react-icons/fa'; // Import icons from Font Awesome library
// import * as OtherIcons from 'react-icons/sl'; // Import icons from another library

// // Combine all imported icons into a single object
// const allIcons = {
//   ...ReactIcons,
//   ...OtherIcons,
//   // Add more libraries here if needed
// };

// // Function to render an icon dynamically based on the icon name
// const renderIcon = (iconName) => {
//   // Get the icon component dynamically based on the iconName
//   const Icon = allIcons[iconName];
  
//   // Check if the icon exists, then render it
//   if (Icon) {
//     return <Icon />;
//   } else {
//     return <div>Icon not found</div>; // Render a fallback if the icon doesn't exist
//   }
// };

// export default renderIcon;
